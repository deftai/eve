---
"eve": minor
---

Add scoped turn and session cancellation across the eve HTTP API, TypeScript client, authored runtime context, delegated agents, and eval controls. Remote agents now consistently use eve's canonical session routes so cancellation follows the same protocol across deployments.
