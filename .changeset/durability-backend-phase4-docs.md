---
"eve": patch
---

Document `experimental.durability.backend`, `eve/durability` exports, and the relationship between durability backends and `experimental.workflow.world`. Clarify that `inMemory()` compiles but does not serve HTTP traffic in v1.
