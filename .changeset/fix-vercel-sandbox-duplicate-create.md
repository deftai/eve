---
"eve": patch
---

Resume an existing named Vercel sandbox when a concurrent session create reports that the sandbox name already exists, so parallel tool calls converge on the same session instead of failing the turn.
